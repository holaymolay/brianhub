# Run daily at 02:15.
# Adjust the env file path if you do not use /etc/brianhub.env.
15 2 * * * cd /opt/brianhub/current && set -a && . /etc/brianhub.env && set +a && /usr/bin/env node scripts/backup-db.js && /usr/bin/env node scripts/restore-check.js >> /var/log/brianhub-backup.log 2>&1
